@*Defining Model*@
@model Producer

@*Defining Viewdata*@
@{
    ViewData["Title"] = "Dashboard";
    ViewData["Section"] = "DVDCopy";
Layout = "_LayoutAdmin";
}

@*Coding*@
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body mx-3">
                <h4 class="card-title mb-2">Update Producer</h4>
                <p class="card-description mb-4"> Update the producer information. </p>
                @*Defining Controller*@
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="row">
                        @*Code for Producer data view in editable fields*@
                        <div class="col-md-6 col-sm-12">
                            <div class="form-group row mb-0">
                                <label class="col-sm-3 col-form-label">Producer Name :</label>
                                <div class="col">
                                    <div id="the-basics">
                                        <input asp-for="ProducerName" style="width: 400px" class="typeahead" type="text"
                                            placeholder="Enter producer name. " />
                                        <span asp-validation-for="ProducerName" class="text-danger"></span>
                                    </div>
                                </div>
                            </div>
                            <br />
                        </div>
                        @*Code for Producer update action buttons*@
                        <div class="col-md-6 col-sm-12">
                            <button value="Update" type="submit" class="btn btn-inverse-success btn-fw mr-4"> Update
                            </button>
                            <a type="button" class="btn btn-inverse-secondary btn-fw" asp-action="Index">Show All</a>
                        </div>
                    </div>
                    <br/>
                </form>
            </div>
        </div>
    </div>
</div>